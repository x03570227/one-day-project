define(function(){
	var form={};
	
	form["textInput"]={
		init:function(id, initValue){
			//初始化表单项及具体内容
		},
		get:function(id){
			//获取表单项的值
		}
	};
	
	form["radio"]={
		init:function(id, initValue){
			//初始化表单项及具体内容
		},
		get:function(id){
			//获取表单项的值
		}
	};
	
	form["checkbox"]={
		init:function(id, initValue){
			//初始化表单项及具体内容
		},
		get:function(id){
			//获取表单项的值
		}
	};
	
	form["textArea"]={
		init:function(id, initValue){
			//初始化表单项及具体内容
		},
		get:function(id){
			//获取表单项的值
		}
	};
	
	form["select"]={
		init:function(id, initValue){
			//初始化表单项及具体内容
		},
		get:function(id){
			//获取表单项的值
		}
	};
	
	form["file"]={
		init:function(id, initValue){
			//初始化表单项及具体内容
		},
		get:function(id){
			//获取表单项的值
		}
	};
	
	return form;
});